"""
run_once.py
-----------
نقطه ورود جایگزین برای اجرا در محیط‌هایی مثل GitHub Actions که به‌صورت
دائمی (long-running) اجرا نمی‌شوند، بلکه هر بار به‌طور جداگانه (مثلاً با
cron هر ۵ دقیقه) صدا زده می‌شوند.

برخلاف main.py که خودش زمان‌بندی می‌کند و برای همیشه در حلقه می‌ماند
(مناسب اجرا روی یک PC/VPS همیشه‌روشن)، این فایل فقط "یک چرخه" تحلیل را
اجرا کرده و خارج می‌شود — زمان‌بندی را در این حالت GitHub Actions
(یا هر cron دیگری) بر عهده می‌گیرد.

استفاده:
    python run_once.py
"""

import main as core

if __name__ == "__main__":
    core.run_analysis_cycle()
